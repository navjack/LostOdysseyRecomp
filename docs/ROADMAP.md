# Roadmap

[简体中文](ROADMAP.zh-CN.md) · [Current status](STATUS.md) · [Changelog](../CHANGELOG.md) · [Historical roadmap snapshot](archive/ROADMAP-2026-09-10.md)

`[ ]` outstanding · `[~]` in progress · `[x]` has bounded evidence. The [public maintainer Project](https://github.com/users/freefrank/projects/3) is the current work-item source of truth. This mirror keeps direction, open work and validation limits; implementation, player acceptance and release status are separate.

## Delivery

[v0.5.6](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.5.6) is the latest release, published on 2026-09-13 (UTC). Release CI, clean package provenance, all 50 payload hashes/CRCs and four anonymous public downloads passed. Historical v0.5.4 artifacts and measurements retain their original identity. See [STATUS](STATUS.md) for source commits, hashes and validation limits.

<a id="v070-frame-generation"></a>
## v0.7.0 frame-generation plan

- [ ] **DLSS-G and FSR Frame Generation:** the Windows PC v0.7.0 plan requires 2× DLSS-G and 2× FSR Frame Generation on both D3D12 and Vulkan. The four provider/API combinations require separate acceptance; D3D12 input diagnosis does not defer Vulkan. P0 fixes SDK, capability, queue and presentation routes; P1 validates native velocity coverage; P2 completes camera, rigid and skeletal motion; P3 freezes color/UI/frame inputs; P4 establishes both APIs; P5/P6 integrate FSR FG and DLSS-G; P7 validates all four combinations; P8 prepares a separately authorized release. FSR Super Resolution and sharpening remain separate Issue #10 scope and are not FG completion. This is planning only: implementation, SDK validation, game tests, player acceptance and publication have not started. See the [full plan](notes/v0.7.0-frame-generation-plan.md).

<a id="v050-pc-graphics"></a>
<a id="next-major-milestone-v050--pc-vulkan-and-direct3d-11"></a>
## PC graphics direction

D3D12 remains the working baseline. Windows Vulkan has bounded RTX 5080 scene validation and remains separate from wider GPU and full-game coverage. Direct3D 11, Linux/Steam Deck and Switch are independent platform work; no completion or release date is implied. The Linux first-playable scope is now implemented on the `linux` branch as a Vulkan-only unbundled ELF launched with `./LostOdysseyRecomp --game <disc>` on host Mesa + SDL2 + X11/XWayland. A WSL2 Manjaro + Mesa Dozen run reached a 1280x720 Vulkan window and exited 0 after the user closed it; native Linux ICD, Steam Deck, packaging and broader game-flow coverage remain open. AppImage and Steam Deck packaging come later; installer, F1 debug menu and updater are outside this branch's scope. See the [Linux port evaluation](notes/linux-port-evaluation-2026-09-13.md).

<a id="near-term-priorities"></a>
## Active work and acceptance boundaries

- [x] **v0.5.4 release:** published 2026-09-11T01:36:30Z from `2ad94d418bb0478417ab9589109f1f685ed92eb3`; CI 34550200618 passed. The 44,237,061-byte ZIP hashes to `104ced8b60c16cd1b9013543a3940c9ed8d7cf904c3d05a6a8ef8d591f51d218`; package provenance, version, all 50 file hashes/CRCs and four anonymous asset downloads passed. This records release delivery only; each item's runtime and player-acceptance limits remain below.
- [~] **Release PPC-library reuse:** direct-main synchronization implementation and local validation are complete. Each authorized PPC synchronization reuses its frozen bundle and pushes that bundle directly to private `main`, without creating a branch. It records the immutable commit receipt locally. Matching input identity is a no-op, unrelated private-repository files are preserved, and the sync uses at most four concurrent retry attempts. Release CI snapshots private `main` over SSH, validates the fingerprint and contract, records the receipt commit locally, then restores the library. Twenty-three PPC sync tests, including six bare-Git integrations, passed once; two synthetic workflow checks and actionlint also passed. Hosted Release CI has not yet validated the producer direct-main synchronization itself; v0.5.6 and v0.5.8 Release CI runs validate consumer use of private main. Earlier opt-in/manual evidence remains historical. No player acceptance is recorded.
- [~] **Linux first playable:** the Vulkan-only unbundled ELF is implemented on the `linux` branch and was compiled and run on WSL2 Manjaro with Mesa Dozen/dzn, reaching a 1280x720 window; the user watched it and closed the window after about 10 minutes. This is bounded WSL evidence only, not native Linux ICD, Steam Deck, packaging, whole-game or published Linux support. The published Windows v0.5.11 ZIP does not include Linux.
- [x] **PPC 0.5.6 fingerprint audit:** [Release CI 34726533463](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34726533463) passed hosted PPC consumption; v0.5.6 was then published after delivery checks. Runner key `d8919775…` retrieved immutable private cache `a6cd91ea…`, restored and checked library `ba3e4c4d…` without generated PPC compilation or a new library link. At the v0.5.6 publication, its non-prerelease was Latest; v0.5.8 is now Latest. Its four public assets passed anonymous HTTP 200, size and hash checks. Producer `50b8ad…`/`6a6ed031…` and the five-line-ending/fourteen-symlink representation proof remain historical. `lo.ppcAutoSync=false`; source-layer key normalization is not implemented.
- [x] **Installer drag crash:** v0.5.4 publicly includes the locally accepted `PostMessageW` repair for the v0.5.3 drag stutter/exit. The message-only HWND DragDispatch case passed 1/1; the legacy fixture remains limited by an unchanged foreground setup assertion. The user accepted the reported drag path on 2026-09-10; broader installer interaction coverage remains separate.
- [~] **Updater simplification:** v0.5.7 is published from 954d0e17; CI 34743383193 passed. PPC key 3260d975 reused the reviewed v0.5.6 library without PPC code changes. Public ZIP, all 50 manifest payloads, standalone updater and two sidecars passed anonymous delivery checks. Synthetic updater evidence remains separate; no real update transaction, visible prompt interaction or player acceptance is recorded.
- [~] **Optional shader collection:** v0.5.3 includes compact opt-in diagnostics and schema 3 binding evidence; Worker schemas 1–4 and the private archive plus ledger are integrated. The compact collection uses a 32-frame/180-second CPU window, at most 24 pairs and 8 bindings, within 32 KiB. All 18 ledger cases remain pending review. Schema 3 covers the two e810 binding pairs only; new candidates still need program review and sufficient final-binding, producer-timing and jitter evidence. Visual repair, player acceptance, broad GPU coverage and all-submitted-draw coverage remain open.
- [~] **Capture export timeout:** two F1 ZIP archives timed out after roughly 60 seconds. The archive worker now waits up to 180 seconds (`60000` → `180000` ms) before treating the child as timed out; its existing process, job-object and `Optimal` compression behavior is unchanged. The v0.5.3 EXE linked successfully, its `build.json` source identity was checked, and the installed replacement hashes to `CF70EA663ED230334145E1135CA97A58DFE3A34C65ED7D43E9E428C41B53270B`; old EXE/metadata are backed up under `out/f1-zip-180s/backup`. The overall build ended with a DXC-copy failure because a source dependency DLL was absent; the already-installed DLL was copied into the build output without recompiling. No timeout recovery test, game run or player acceptance is recorded; v0.5.4 package and public-download verification passed.
- [~] **4K TAA and ground/shadow feedback:** captures 17468–17470 came from an older executable that predates the four-path c7 repair. They do not reopen the later accepted four-path Sol repair, diagnose a new cause or establish broad visual coverage. Continue scene, hardware and reporter-specific validation through the Project.
- [x] **Kanto armored-soldier lighting flicker:** v0.5.7 is published from 954d0e17; CI 34743383193 and all package delivery checks passed. The user accepted the fifth candidate in the reported encounter only; this does not establish all-scene/full-game coverage. A recurrence reopens this item.
- [~] **PPC generated-source provenance guard:** the generated-tree provenance sub-scope is complete: `ppc_codegen generate` produced 247 C++ units with 843 `.u32` and no `.u64` selectors, while 246 instruction-stream hashes remained unchanged. Seven guard fixtures passed for stale output, input/tool/context drift and return-0-without-output recovery; the historical 3,258/109/843-table-44,523 evidence was reused without rerun. CMake configuration and the compile-order guard dependency are verified; the broader PPC-contract audit remains open. Issue #14 investigation distinguishes the original historical v0.5.0 log (LR `829DFDCC`, CTR `829DFEF0`) from a second historical v0.4.2 log (LR `823CB53C`, CTR `0`). The latter's guest virtual-call site `823CB538` resembles the Issue #12 material-call analysis, but no object-freeing capture establishes the same cause. `gc_render_flush.cpp` exists at `17e3ab7`; no new runtime validation, current-version reproduction, reporter acceptance or release evidence exists.
- [~] **New gameplay investigations (#15 and #16):** #15's source-v0.5.4 log continued rendering at 60 fps with audio until the user closed the process; it later loaded the same save and a second save did not hang. Its directory name `0.5.0` is not version evidence. Two compiler-DLL hashes match the v0.5.4 manifest, while other components remain unknown. The updater investigation found that `StageArchive` omits `manifest.json` from `ApplyPlan`, leaving an old manifest after update. Its associated repair is validated: the current-worktree clang-cl `/O2 /MT` targeted build succeeded and `--manifest-transaction` completed three scenarios with zero failures, covering plan round trip, replacement plus post-apply rollback, failure after manifest replacement rollback, and manifest-tamper rejection. That original updater-only validation did not run the helper/game or establish player acceptance; the implementation is now included in published v0.5.6. The up-to-date path still compares only version rather than every installed hash, so mixed DLL/resource installs remain unexcluded and the Issue #15 hang cause remains unknown. #16 implementation and bounded local validation are complete; reporter validation remains pending. Final branch-native-r1 EXE `f2015ad7…` passed final-freeze-01 on D3D12/Asian Disc 3 from user09 without skipping the King freeze. At 1527.792s raw `0A33A8C0` used missing-particle-shader fallback; stable memory matched `xf_shd_aniflz.freeze`, GUID `0fd4ca6d4bb67581c9c9f5b8f0af62c0`, child table count 16 and particle shader 0. All 180 PNGs completed (shots 45508 freeze, 45823 dialogue, 47673 exterior); map229 returned at 1733.599s, and shots 53430-53517 show 30-tick down-input movement/camera change. This run has no coordinate telemetry. Both native checkpoints reload and the original seed is unchanged. Reporter/player acceptance and Vulkan/other-region coverage remain pending. The implementations were subsequently committed, pushed and included in published v0.5.6; reporter acceptance remains separate.
- [~] **Issues #14–#16 local merge record:** local commit `2019cd017ab939d0b728cec340f7835c2082e615` records this task's triage, updater-manifest repair and particle compatibility repair/evidence; it is included in published v0.5.6 from `7124f4b…`. [Release CI 34726533463](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34726533463), package provenance/50 member hashes and four anonymous public-download checks passed. The individual investigation, reporter-acceptance and coverage limits above remain unchanged.
- [~] **#16 main 0.5.6 replay:** the normal main Release build (`d50c240d…`, 173.782 s) passed the complete user09 target replay on D3D12/local Asian Disc 3, without diagnostic objects or a binary bridge. Native F1 won twice; the freeze was not skipped, fallback hit raw `0a54ce40` at 1759.793 s, story continued, map229/menu/30-tick movement passed, and 360 screenshots completed. The repair is included in published v0.5.6; CI/package/public-download evidence verifies delivery only and did not repeat gameplay. Reporter/player acceptance, full-game, Vulkan and other-region evidence remain pending. The older branch-native-r1 v0.5.4 pass remains separate.
- [ ] **Presentation and input:** fullscreen, Alt+Enter, mixed-DPI and mouse acceptance remain open. Preserve normal aspect-ratio black bars when assessing reported underscan.
- [ ] **Gameplay and stability:** later progression, save/readback, encounters, long-running stability, remaining rendering reports and cross-GPU/package evidence remain tracked work. Existing bounded repairs do not prove a complete playthrough.
- [~] **Issues #6 and #22 startup diagnostics:** v0.5.11 was publicly released 2026-09-14T02:09:50Z from `624729c` with [CI 34797755460](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34797755460) successful. The 44,304,683-byte ZIP SHA-256 is `5de068c4e77c82feb0bbe7cfcf1dacbca3d44aa94bde064f7f59f5ad6944e132`; clean provenance, 50 payload hashes/CRC and all four anonymous public asset checks passed. The selected logging fixtures and bounded D3D12/Vulkan log checks are reused, not rerun. Both trackers remain OPEN/In Progress: root causes and reporter acceptance are unknown/pending. #6's v0.5.6 association is historical only. See [v0.5.11](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.5.11).
- [~] **Assembly-level performance analysis:** the external Win64 tool now records wall-clock RIP snapshots, resolves DbgHelp PDB symbols/source locations, and generates Capstone x64 HTML/JSON hotspots, thread CPU-time/filtering and function self-sample rankings. Seven focused report cases, an MSVC Release build and a synthetic end-to-end capture passed. Two 2026-09-11 isolated gameplay captures on published v0.5.4 EXE `3c3b4073…` recorded user00 walking (5,092 samples, 0 failed, hottest TID 8.11 s CPU) and user01 city walking (10,736 samples, 0 failed, hottest TID 7.27 s CPU); both `xenon_scr.fpd`, about 36–44 fps, 1,900–2,300 draws/frame, 1280×720 frontbuffer. Hottest threads mix unresolved EXE samples with `NtWaitForSingleObject` and AMD/D3D12. No matching game PDB, GPU instruction profiling, call stacks or player acceptance. See [gameplay captures](notes/asm-profiler-gameplay.md). Its PPC annotation is generated-source context, not an exact guest PC, and requested sampling intervals are not actual frequency.
- [~] **Performance and shader startup:** local `main` commit `ae287f2` records the completed bounded Hidden-city segment: the 41.8241 ms vertex hitch was `std::unordered_map` insertion/rehash, rather than `CopySwapped`; a 65,536-entry reserved dense metadata cache with 16-candidate LRU eviction preserves 1280×720 D3D12 AA=3, quality, two slots and arena/wait policy; and `LoVertexCacheTest` passed once with 3,569,548 checks. The final same-EXE run moved only driver input/screenshot-request files to TEMP, reducing observed post-present max 412.9283→0.4193 ms without proving a filesystem cause. Across 1,790 city frames mean was 59.651463 FPS; the 1,201-frame fixed window reached 59.918376 mean FPS, 54.494611 1% low, 50.374688% above 16.67 ms, 22.5913 ms max present, 2.2981 ms max vertex, zero rehashes and no draw over budget. The ≥58 mean target is met, while all-city 1% low 45.989346 and 43.0117 ms max present retain a city-entry load/bind tail. This is near-60 route evidence, not an every-frame 60 lock, strict S4/full-game pass or player acceptance. The implementation is included in published v0.5.6; hosted CI, package and public-download verification passed. The cited performance measurements retain their historical executable identity. Original saves remained unchanged and local EXE SHA-256 is `9D9460248FEB72AC7239ABD40AC1DA6619847F176CF4AA38AD6F725C6923852B`. See [vertex-stage report](../out/perf-ring/vertex-stage/REPORT.md).
  **v0.5.9 publication and Project classification:** v0.5.9 published 2026-09-13T19:50:00Z from `d26ee8b021784d7232b5319d816227867f98050d`; CI 34778434518, package provenance, all 50 payload hashes and four anonymous public downloads passed. The Project Release field for this continuing item is v0.5.9. Earlier candidate measurements are explicitly pre-release history, not clean-release benchmarks. 4K60, Vulkan 1080p60 at 15 W and player acceptance remain pending.

  Vulkan v0.5.9 published: depth-clear and draw-state/cache changes shipped with existing validation reused; CI 34778434518 and all four public asset downloads verified. Bounded 4K/60-W result: 7.50 to 43.48 FPS; binding-only follow-up establishes no overall gain. 4K60, 1080p60 at15W and player acceptance remain pending. See [Vulkan evidence](notes/vulkan-depth-clear-performance-2026-09-13.md).


- [~] **v0.5.8 TAA and CPU inclusion:** [v0.5.8](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.5.8) was publicly published 2026-09-13T15:11:11Z from 6e6f11cf; [Release CI 34764115203](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34764115203) passed. The clean source-0.5.8 package passed ZIP CRC, all 50 payload hashes, and anonymous verification of all four public assets (bytes, SHA, sidecars and API digests). It includes the TAA cde8b50 resolution and the SIMD/LO_QUERY_TRACE presence-cache fixes. TAA scene visual acceptance and the broader performance/shader-startup investigation remain open; the historical v0.5.6 bounded-city release evidence and earlier 40-W windows retain their original identities.

- [~] **Vulkan pixel-shader LoopEnd predicate exit:** v0.5.7 is published from 954d0e17; CI 34743383193 and all package delivery checks passed. Prior bounded LoopEnd guard, 64-lane GPU/reference and cache evidence is retained. Complex loops, actual game GPU power/cross-scene validation and player acceptance remain pending.
- [~] **Adjacent resolve-copy elision:** v0.5.7 is published from 954d0e17; CI 34743383193 and all package delivery checks passed. Prior CPU and D3D12/Vulkan FP16 fixture evidence is retained. Actual game hits, cross-scene validation and player acceptance remain pending.

<a id="current-feedback-and-regression-work"></a>
## Validated scope retained for reference

The user accepted the limited D3D12/Vulkan real-scene boundary on 2026-09-08. The four c7 paths were accepted in one Ghost Town scene over about 11.37 seconds; that is not continuous-video, cross-scene, cross-GPU or full-game coverage. Earlier published repairs and their evidence remain available through [STATUS.md](STATUS.md), [CHANGELOG.md](../CHANGELOG.md) and the historical snapshot.

## Navigation

Use the [Project](https://github.com/users/freefrank/projects/3) for item status, dependencies and detailed outstanding requirements. Use [STATUS.md](STATUS.md) for current validation and release evidence, [CHANGELOG.md](../CHANGELOG.md) for published changes, and [the archived roadmap](archive/ROADMAP-2026-09-10.md) for the retained detailed history.

<!-- Compatibility anchors retained for existing Project and note links. -->
<a id="published-milestone-v042--repairs-and-validation"></a>
<a id="phase-1-produce-compilable-output"></a>
<a id="phase-2-reach-the-main-menu"></a>
<a id="phase-3-work-toward-a-complete-playthrough"></a>
<a id="phase-4-modernization"></a>
<a id="phase-5-optional-exploration"></a>
